[pytest]
# 测试用例发现规则
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 测试报告配置
addopts =
    -v
    --alluredir=./allure-results
    --clean-alluredir

# 禁用警告
filterwarnings =
    ignore::DeprecationWarning
